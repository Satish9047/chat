{
  "name": "chat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "concurrently \"npm run app\" \"npm run server\"",
    "install-app": "npm install --prefix app",
    "install-server": "npm install -- prefix server",
    "install": "npm run install-app && npm run install-server",
    "app": "npm run dev --prefix app",
    "server": "npm start --prefix server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}
